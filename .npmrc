confirm-modules-purge=false
ignore-scripts[]=better-sqlite3

# Skip Electron binary download in CI / cloud builds (Pages, Render, GitHub Actions).
# The desktop app binary is downloaded on first run locally, not during pnpm install.
ELECTRON_SKIP_BINARY_DOWNLOAD=1
